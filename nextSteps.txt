-create a route that accepts either parameters or body for query and language
    --call api for translation, return translation

--Create 6 units of words
    -each unit has 3 sub units
        -1: flashcards to learn words
        -2: translate from english into target language by matching
        -3: translate from target language into english by typing


--create MVP database schema

-create a route that accepts unit/subunit request, and returns the materials